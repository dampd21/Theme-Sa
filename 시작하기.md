# Cloudflare 시작하기

**홈페이지: https://theme-sa.dampd21.workers.dev/**

2026-09-12 배포 완료. 등록된 공용 비밀번호로 로그인하면 됩니다. 아래 관리자 설정은 재설정·유지보수용입니다.

방문자는 **공용 비밀번호만 입력**합니다. 기본 12시간 동안 로그인 상태를 유지하고, GitHub 토큰은 Cloudflare 서버에만 보관합니다.

## 확장 기능 사용하기

[38개 기능·직함·조직도 안내](확장기능-안내.md)를 참고하세요.

- 로그인 후 상단 활동 프로필을 선택합니다. 선택 이름은 본인 인증이 아닙니다.
- **기록 저장하기 → 변경 비교 → 확인하고 저장**으로 팀에 공유합니다.
- **직함·조직도 → 직함 만들기**에서 상위 직함을 정하고, **팀원 수정 → 직함 부여**에서 담당자를 배치합니다.
- 배포 전에는 입력을 저장하고, 업데이트 후 새로고침·재로그인하세요.

## 관리자 설정

GitHub의 **Theme-Sa → Settings → Secrets and variables → Actions**에서 입력합니다.

### Secrets 탭 — 3개

| 이름 | 입력할 값 |
|---|---|
| `CLOUDFLARE_API_TOKEN` | Cloudflare의 Workers 배포용 API 토큰. 사용할 계정 하나로 범위 제한 |
| `SITE_PASSWORD` | 팀 공용 비밀번호, 5~200자 허용 (12자 이상 권장) |
| `DATA_REPO_TOKEN` | GitHub `Theme-Sa-data` 하나만 허용한 Fine-grained 토큰, Contents: Read and write |

### Variables 탭 — 1개

| 이름 | 입력할 값 |
|---|---|
| `CLOUDFLARE_ACCOUNT_ID` | Cloudflare 대시보드의 32자리 Account ID |

기존에 `SITE_PASSWORD`와 `DATA_REPO_TOKEN`을 올바르게 등록했다면 유지해도 됩니다. **Cloudflare 배포 토큰과 GitHub 기록 토큰은 서로 다른 값입니다.** 실제 값은 채팅에 보내지 말고 설정 화면에 직접 입력하세요.

## 어디서 구하나요?

- Cloudflare Account ID: 사용할 Cloudflare 계정의 대시보드에서 확인
- Cloudflare API 토큰: My Profile → API Tokens → Create Token → Edit Cloudflare Workers 템플릿. 대상 계정 하나로 제한
- GitHub 기록 토큰: GitHub 개인 Settings → Developer settings → Personal access tokens → Fine-grained tokens. Resource owner `dampd21`, 저장소 `Theme-Sa-data` 하나, Contents 쓰기 권한

## 배포

새 코드가 `main`에 반영된 뒤:

**Actions → Deploy Theme-Sa to Cloudflare → Run workflow**

성공한 배포 로그 또는 Cloudflare **Workers & Pages → theme-sa**에서 실제 `workers.dev` 주소를 확인합니다. 그 주소가 새 홈페이지입니다.

현재 확인된 주소는 **https://theme-sa.dampd21.workers.dev/** 입니다. 다른 계정으로 이전하면 새 배포 결과의 URL을 사용하세요.

## 기존 사이트와 기록

- 기존 `Theme-Sa-data/data/team.json`을 그대로 사용합니다.
- 새 Cloudflare 사이트가 정상 작동하기 전에는 기존 GitHub Pages를 삭제하지 않습니다.
- 새 주소에서 저장·불러오기·새로고침 후 로그인 유지가 확인되면 그 주소를 공유합니다.
- 과거 Pages 배포 워크플로를 재실행하지 마세요. 이번에는 **Cloudflare 워크플로만** 사용합니다.

## 기억할 점

- 사용자에게 GitHub 토큰이 전달되지 않습니다. 예전 브라우저 복호화 방식은 제거했습니다.
- 새로고침해도 로그인은 유지되지만, 로그아웃·12시간 만료·비밀번호 변경·새 배포 후에는 다시 로그인합니다.
- 같은 비밀번호를 쓰는 사람은 모두 같은 팀 기록 관리 권한을 갖습니다.
- 우클릭/F12 제한은 간단한 UI 제한일 뿐, 소스 숨김 보안 기능이 아닙니다.
- 상세 집주소는 저장하지 마세요. GitHub는 삭제 전 내용도 이력에 남깁니다.

더 자세한 안내는 **README.md**, 보안 설명은 **SECURITY.md**에 있습니다.
