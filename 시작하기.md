# 공용 비밀번호로 바꾸기 — 관리자 최초 설정

방문자는 **비밀번호 한 칸만 입력**합니다. 방문자마다 GitHub 계정이나 토큰을 만들 필요는 없습니다.

외부 서버는 사용하지 않습니다. 대신 **비밀번호를 아는 모든 사람에게 같은 기록 저장소의 읽기·쓰기 연결 권한이 전달**됩니다. 믿을 수 있는 그룹 안에서만 사용하세요.

## 관리자 설정 두 개

화면 저장소 **Theme-Sa → Settings → Secrets and variables → Actions → Secrets**에 등록합니다.

| 이름 | 값 |
|---|---|
| `SITE_PASSWORD` | 팀에 알려줄 공용 비밀번호, 최소 12자. 긴 고유 비밀번호 권장 |
| `DATA_REPO_TOKEN` | `Theme-Sa-data` 하나만 선택하고 Contents를 Read and write로 지정한 Fine-grained 토큰 |

- **Classic 토큰은 공용 연결키로 사용하지 않습니다.**
- Secrets에 직접 입력하고 채팅·코드·Variables에 넣지 마세요.
- 연결키 발급: GitHub 개인 설정 → Developer settings → Personal access tokens → Fine-grained tokens. 소유자 `dampd21`, 선택 저장소는 `Theme-Sa-data` 하나입니다.

## 배포

이 수정본을 `main`에 반영한 뒤 **Actions → Deploy static content to Pages → Run workflow**를 실행합니다.

배포가 성공하면 아래 주소에서 설정한 비밀번호로 입장합니다.

**https://dampd21.github.io/Theme-Sa/**

Secrets가 없으면 배포를 중단하고 이전에 성공한 사이트를 유지합니다. 아직 설정하지 않았다면 운영 사이트가 이전 토큰 로그인 화면인 것이 정상입니다.

## 알아둘 점

- 비밀번호와 연결키를 프론트에 평문으로 넣지 않습니다. 암호화한 연결키를 배포하고 브라우저에서 비밀번호로 엽니다.
- 비밀번호를 아는 사람은 연결키도 알아낼 수 있습니다. 사용자별 권한을 분리하는 서버 인증과 다릅니다.
- F12·우클릭 제한은 간단한 사용 제한이며 **소스를 완전히 숨기지 못합니다**. 입력창에서는 붙여넣기를 허용합니다.
- 비밀번호를 바꾸며 기존 사람의 접근도 끊으려면 **연결 토큰도 새로 발급하고 이전 토큰을 폐기**해야 합니다.
- 토큰 만료 시 Secret을 새 토큰으로 교체한 뒤 재배포합니다.
- 새로고침하거나 로그아웃하면 다시 비밀번호를 입력합니다.
- 상세 집주소는 저장하지 마세요. GitHub의 과거 커밋 이력에도 남습니다.

자세한 설명은 `README.md`와 `SECURITY.md`에 있습니다.
