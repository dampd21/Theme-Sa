# 비밀 기록실 · GitHub 배포 시작하기

**GitHub만 사용하는 공동 편집판을 준비했습니다.** 외부 데이터베이스나 중계 서버 없이, 비공개 GitHub 저장소의 최신 기록을 읽고 페이지에서 바로 저장합니다.

> 이 버전은 **공통 비밀번호가 아니라 각자의 GitHub 개인 접근 토큰**으로 연결합니다. Actions Secrets의 비밀번호를 사이트에 심지 않습니다.
>
> **GitHub 계정은 만 13세 이상이어야 합니다.** 조건을 충족하지 않는 친구에게 보호자의 계정·토큰을 공유하지 마세요. 그런 친구들이 직접 로그인해 편집하려면 별도 로그인 서버 방식이 필요합니다.

## 준비물

- 관리자와 참여자 각자의 GitHub 계정
- 팀용 GitHub Organization과 조직 구성원으로 초대된 친구들
- **공개 화면 저장소 1개 + 비공개 기록 저장소 1개**

## 1. 비공개 기록 저장소 만들기

팀 조직 아래 `spirit-team-data` 같은 저장소를 **Private**으로 만드세요. **Add a README file**을 선택해 `main` 브랜치를 만듭니다.

편집할 친구에게는 이 저장소의 **Write 권한**을 부여하세요. Fine-grained 토큰의 협업자 제약 때문에, 친구들을 단순 외부 협업자가 아닌 **조직 구성원(Member)**으로 초대하는 구성을 권장합니다.

## 2. 화면 저장소에 배포 파일 올리기

별도의 공개 저장소(예: `spirit-archive`)를 만듭니다. ZIP을 풀고 아래 구조를 유지한 채 **저장소 루트**에 올리세요.

```text
.github/workflows/pages.yml
site/index.html
site/config.js
site/.nojekyll
README.md
SECURITY.md
시작하기.md
```

`.github` 폴더가 숨겨져 누락되지 않게 주의하세요. **이전에 만든 개인정보 포함 로컬 HTML이나 팀 백업 JSON을 공개 저장소에 올리면 안 됩니다.**

## 3. Actions Variables 두 개 설정하기

**화면 저장소 → Settings → Secrets and variables → Actions → Variables**

| 변수 | 값 |
|---|---|
| `ARCHIVE_OWNER` | 비공개 기록 저장소를 소유한 조직명 |
| `ARCHIVE_REPO` | 비공개 기록 저장소 이름, 예: `spirit-team-data` |

선택 변수: `ARCHIVE_BRANCH`(기본 `main`), `ARCHIVE_PATH`(기본 `data/team.json`).

여기에는 **비밀번호나 토큰을 넣지 마세요.** 이 저장소 좌표는 공개 설정으로 배포됩니다. 공통 비밀번호용 Secrets는 사용하지 않습니다.

## 4. Pages 배포하기

1. 화면 저장소의 **Settings → Pages → Source: GitHub Actions**.
2. **Actions → Deploy spirit archive to GitHub Pages → Run workflow**.
3. 성공한 배포 결과의 `github-pages` 주소를 엽니다.

일반적인 주소는 `https://조직명.github.io/spirit-archive/`입니다. 실제 주소는 배포 결과에서 확인하세요.

## 5. 각자 토큰으로 연결하기

본인 GitHub 설정에서 **Developer settings → Personal access tokens → Fine-grained tokens**로 이동합니다.

- Resource owner: 팀 조직
- Repository access: **Only select repositories** → 비공개 기록 저장소 하나만
- Repository permissions: **Contents → Read and write**
- 만료 기간: 7일 등 짧게 설정
- 조직 정책이 요구하면 관리자 승인

각자 발급받은 토큰을 **직접 배포한 기록실의 로그인 화면**에 입력합니다. 관리자 토큰을 나눠주는 방식이 아닙니다. 첫 연결에서 기록 파일이 없다면 **빈 기록실 만들기**를 선택하세요.

## 사용 방식

- 입력 후 **GitHub에 저장**을 눌러야 다른 친구에게 반영됩니다.
- 최신 기록은 접속할 때 불러오며, 편집하지 않을 때 약 **30초마다** 다시 확인합니다.
- **최신 기록** 버튼으로 즉시 확인할 수 있습니다.
- 동시에 수정하면 충돌을 알리고, 기존 기록을 덮어쓰지 않습니다.
- 토큰과 기록은 브라우저 저장소에 남기지 않습니다. **새로고침하면 다시 연결해야 합니다.**
- 이전 로컬판 기록은 JSON으로 내보낸 뒤 새 페이지의 **저장 · 불러오기**에서 옮길 수 있습니다.
- 상세 집주소는 넣지 마세요. GitHub는 삭제 전 내용도 커밋 이력에 남깁니다.

**실제 계정 배포는 아직 수행하지 않았습니다.** 모의 GitHub API로 2인 공동 저장·충돌·권한 거절을 테스트했습니다. 설정 후 가상 기록으로 먼저 확인해 주세요.

자세한 단계와 오류 해결은 **README.md**, 개인정보·권한 관리는 **SECURITY.md**에 있습니다.
