# 안전한 기록실 운영 안내

## 신뢰 경계

- 공개 HTML은 로그인 화면과 프로그램일 뿐이며, 공개 코드 자체를 비밀로 숨기지는 않습니다.
- 실제 권한 경계는 GitHub의 비공개 저장소 권한과 관리자 본인의 GitHub 개인 접근 토큰입니다.
- 토큰은 GitHub API(`https://api.github.com`)의 Authorization 헤더로만 전송합니다. URL, 파일, localStorage, sessionStorage에는 넣지 않습니다.
- 토큰과 불러온 팀 기록은 열린 페이지의 메모리에 존재합니다. 악성 브라우저 확장, 개발자 도구 접근, XSS, 기기 탈취로부터 완전한 보호를 제공하지는 않습니다.
- 앱은 외부 CDN·분석 도구를 쓰지 않으며, 외부 연결을 GitHub API로 제한하는 CSP를 포함합니다. GitHub 설정·기록 링크를 여는 것은 사용자 동작입니다.
- 입력·불러온 텍스트는 HTML에 삽입하기 전에 이스케이프하고 스키마를 검증합니다. 이름·메모에 HTML을 넣어도 실행하지 않습니다.

## 관리자 체크리스트

- [ ] 화면용 공개 저장소와 기록용 비공개 저장소를 분리했습니다.
- [ ] 기록 저장소에는 팀 JSON 외에 다른 중요한 파일·자격증명이 없습니다.
- [ ] 화면 코드 변경 권한은 신뢰할 수 있는 관리자에게만 있습니다.
- [ ] 관리자 본인의 계정과 토큰을 사용하며 GitHub 이용 조건을 준수합니다.
- [ ] 관리자에게 전용 비공개 기록 저장소의 필요한 권한만 부여합니다.
- [ ] 평소에는 해당 기록 저장소 하나만 선택한 Fine-grained 토큰과 짧은 만료 기간을 사용합니다.
- [ ] 어떤 종류의 토큰도 배포 파일에 넣지 않았습니다. Classic 토큰을 입력해서 사용할 경우 넓은 접근 범위와 폐기 필요성을 이해합니다.
- [ ] Actions에는 토큰·비밀번호를 HTML로 내보내는 작업이 없습니다.
- [ ] 화면의 저장소 주소 설정은 공개된다는 점을 알고 있습니다.
- [ ] 실제 이름·학교·상세 주소 대신 별칭·가상 설정 위주로 기록합니다.
- [ ] 기록에 포함되는 사람의 동의를 받았습니다.
- [ ] GitHub 커밋 작성자의 계정 정보도 커밋 이력에 남는다는 점을 안내했습니다.
- [ ] 새 기능 배포는 신뢰할 수 있는 코드인지 검토하고 민감하지 않은 테스트 데이터로 먼저 확인합니다.

## 토큰 유출 시

1. 해당 사용자가 GitHub 설정에서 토큰을 즉시 Revoke합니다.
2. 비공개 저장소의 커밋 이력·파일 변경을 확인합니다.
3. 필요하면 해당 사용자의 저장소 접근을 일시 해제합니다.
4. 이미 노출된 데이터는 토큰을 폐기해도 회수되지 않습니다. 영향을 받은 사람에게 알리고 대응합니다.
5. 새 토큰은 범위를 다시 제한해 발급합니다. 기존 토큰을 다시 사용하지 않습니다.

## 개인정보 삭제

기록 삭제 버튼은 현재 JSON에서 항목을 제거합니다. **GitHub의 과거 커밋·클론·백업·열람한 사본까지 지우지 않습니다.**

민감한 정보가 이미 저장소에 들어갔다면 현재 파일을 수정하는 것으로 끝내지 말고, GitHub의 민감 데이터 제거 절차와 저장소 접근 범위를 확인하세요. 기존 클론이나 백업 사본도 별도로 다뤄야 합니다. 이 앱은 이력을 강제 재작성하거나 영구 삭제를 자동 수행하지 않습니다.

## 공동 편집의 한계

- 저장은 파일 SHA 기반 낙관적 동시성 제어를 사용합니다. 오래된 SHA로 덮어쓰려 하면 GitHub가 거절합니다.
- 자동 갱신은 30초 간격 확인이며 웹소켓 실시간 공동 타이핑이 아닙니다.
- 네트워크 오류 시 저장 성공 여부가 모호할 수 있습니다. 초안을 내려받고 최신 기록을 확인하세요.
- 미저장 초안은 메모리에만 있으며 창을 닫으면 사라집니다. 초안 내보내기는 기본적으로 주소를 제외합니다.
- 수동으로 예전 JSON을 복원하는 것은 최신 기록 전체를 의도적으로 교체하는 동작입니다. 경고를 확인해야 합니다.

## 연령과 계정 공유

GitHub 약관은 사용자에게 만 13세 이상을 요구하며, 한 개인 계정의 로그인을 여러 사람이 공유하는 것을 허용하지 않습니다. 이 조건을 충족하지 않는 친구에게 관리자·보호자의 토큰을 나눠주는 방식으로 우회하지 마세요. 연령 조건을 충족하지 않는 사용자가 직접 편집해야 한다면 별도 서비스의 적절한 로그인 구조를 검토해야 합니다.

## 운영 전 검증

모의 API 테스트와 별도로 실제 배포 환경의 저장소 권한, 브랜치 규칙, Pages 설정을 점검해야 합니다. 배포 검증에 사용한 임시 자격증명은 홈페이지에 저장하지 않습니다. 이 문서는 보안 보증이나 법률 자문이 아닙니다.
