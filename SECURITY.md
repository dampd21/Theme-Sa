# 공용 비밀번호 기록실의 보안 전제

## 무엇을 보호하고 무엇을 보호하지 않나요?

이 정적 사이트는 로그인 서버가 아닙니다. 배포 시 전용 GitHub 토큰을 공용 비밀번호로 암호화하고, 방문자의 브라우저에서 복호화합니다.

- 평문 비밀번호나 평문 GitHub 토큰은 배포 파일에 넣지 않습니다.
- 배포된 `vault.json`은 누구나 내려받을 수 있는 암호문입니다.
- 비밀번호를 모르는 사람은 정상적으로 연결키를 복호화할 수 없습니다. 다만 암호문을 내려받아 비밀번호를 오프라인으로 추측할 수 있습니다.
- **비밀번호를 아는 모든 사람은 연결키를 얻을 수 있습니다.** UI 제한을 넘어서 토큰 권한 범위 안에서 GitHub API를 직접 사용할 수도 있습니다.
- 암호화는 토큰을 로그인한 사용자에게 숨겨 주지 않습니다.
- GitHub의 비공개 저장소 권한이 실제 데이터 접근을 통제합니다. 앱에서 허용한 동작만으로 API 사용 범위를 강제하지 않습니다.
- 개인별 로그인·역할·권한·퇴출·감사 기록은 제공하지 않습니다. 비밀번호를 공유한 사람 모두에게 동일한 읽기·쓰기 권한을 주어도 되는 경우에만 사용하세요.

## 연결키 최소 권한

- `Theme-Sa-data` **하나만** 선택한 Fine-grained 토큰을 사용합니다.
- 필요한 권한은 Contents의 Read and write입니다. 화면 저장소, 다른 개인 저장소, 관리 권한을 포함하지 마세요.
- Contents 권한은 해당 저장소의 파일들에 적용되며 `data/team.json` 한 파일로만 제한되는 것은 아닙니다. 데이터 전용 저장소에 다른 중요한 파일이나 자격증명을 넣지 마세요.
- 범위가 넓은 Classic 토큰은 빌드 도구에서 거절합니다.
- 토큰의 실제 선택 저장소 범위는 관리자가 GitHub 설정에서 확인해야 합니다. 토큰 접두어만으로 다른 저장소까지 허용했는지를 판별할 수는 없습니다.
- 전용 계정·연동 정책과 GitHub 이용 조건을 준수하고, 개인 GitHub 로그인 비밀번호나 광범위한 관리자 자격증명을 공유하지 마세요.

## 암호화

- PBKDF2-SHA256, 600,000 iterations
- 무작위 16-byte salt, 32-byte 키
- AES-256-GCM, 무작위 12-byte IV, 128-bit 인증 태그
- 고정된 형식 식별자를 AAD로 인증
- 배포마다 새로운 salt와 IV를 사용
- Node 내장 crypto와 브라우저 Web Crypto API 사용

이 설정만으로 약한 비밀번호를 강한 비밀번호로 만들 수는 없습니다. 빌드는 최소 12자를 요구하며, 관리자는 사전 단어나 규칙적인 숫자 대신 길고 고유한 비밀번호를 선택해야 합니다.

## 소스 보기 억제

F12, Ctrl/Cmd+U, 일부 개발자 도구 단축키와 일반 영역의 우클릭을 억제합니다. 입력창에서는 붙여넣기를 위해 메뉴를 허용합니다.

**이 기능은 보안 기능이 아닙니다.** 이미 열린 개발자 도구, 브라우저 메뉴, JavaScript 비활성화, 다른 프로그램이나 직접 HTTP 요청으로 소스를 볼 수 있습니다. 화면 코드와 암호문은 공개되어 있다는 전제로 운영해야 합니다.

## 저장과 세션

비밀번호와 복호화한 연결키, 팀 기록은 페이지 메모리에만 유지합니다. 앱은 localStorage나 sessionStorage에 보관하지 않습니다. 사용자의 브라우저 비밀번호 관리자가 입력값을 저장하는지는 별도 브라우저 설정에 따릅니다.

기록은 GitHub로 전송되며 커밋 이력에 남습니다. 네트워크 오류나 SHA 충돌 시 초안은 페이지에만 남으므로 닫기 전에 보관해야 합니다.

## 비밀번호 변경·유출 대응

비밀번호만 바꾸면 기존 암호문이나 연결키를 보관한 사람의 접근을 끊을 수 없습니다.

1. 유출된 연결키는 즉시 GitHub에서 폐기합니다.
2. 저장소 파일과 커밋 이력을 점검합니다.
3. 새 전용 토큰과 새 공용 비밀번호를 발급합니다.
4. Actions Secrets의 두 값을 바꾸고 다시 배포합니다.
5. 이전 토큰이 실제로 폐기됐는지 확인합니다.
6. 이미 내려받거나 열람한 기록은 회수할 수 없습니다. 영향을 받은 사람에게 알리고 필요한 조치를 취합니다.

평상시 접근자를 바꿀 때도 새 비밀번호와 새 토큰을 함께 사용하고 이전 토큰을 폐기하세요. 과거 Pages 산출물·캐시·사용자 사본이 존재할 수 있습니다.

## 관리자 체크리스트

- [ ] 기록 저장소는 Private이며 화면용 공개 저장소와 분리되어 있습니다.
- [ ] 공용 연결 토큰은 데이터 저장소 하나에만 접근합니다.
- [ ] 비밀번호는 길고 추측하기 어려우며 다른 서비스에서 사용하지 않습니다.
- [ ] 비밀번호를 공유하는 모든 사람이 기록을 직접 수정할 권한을 가져도 됩니다.
- [ ] 화면 코드와 워크플로 수정 권한은 신뢰하는 관리자에게만 있습니다.
- [ ] Secrets를 로그나 공개 파일로 출력하지 않습니다.
- [ ] `site/vault.json`은 CI에서 생성하며 소스 Git 이력에는 커밋하지 않습니다.
- [ ] 토큰 만료일을 관리하며 교체 후 재배포 절차를 알고 있습니다.
- [ ] 상세 집주소를 저장하지 않고, 실제 개인정보는 필요한 범위와 동의 아래에서만 기록합니다.
- [ ] 삭제해도 GitHub의 과거 커밋 이력과 사본까지 없어지지는 않는다는 점을 이해합니다.

공용 로그인 코드는 모의 GitHub API로 검증되었습니다. 운영 Secrets를 설정한 뒤 실제 배포 환경에서 저장·불러오기를 확인해야 합니다.
