# Cloudflare 기록실 보안 안내

## 인증 경계

비밀번호 확인과 GitHub 읽기·쓰기는 Cloudflare Worker에서 수행합니다. 브라우저에는 GitHub 연결 토큰을 평문이나 암호문으로 전달하지 않습니다. 이전 `vault.json` 방식은 제거했습니다.

관리자의 선택에 따라 5~200자 비밀번호를 허용합니다. 5자리 비밀번호는 추측 공격에 취약하며 IP별 시도 제한만으로 분산 공격을 막을 수 없습니다. 보안상 12자 이상의 길고 고유한 비밀번호를 권장합니다.

공용 비밀번호를 아는 사람은 모두 같은 기록 관리 권한을 갖습니다. 개인별 사용자·역할·권한·감사 기록을 구분하는 시스템은 아닙니다.

## 확장 기록과 선택 프로필

활동 프로필은 공용 로그인 후 선택한 이름입니다. 다른 사람의 프로필도 선택할 수 있으므로 개인 신원, 중복 계정 방지, 엄격한 1인 1표를 보장하지 않습니다. 직함과 조직도는 표시용이며 접근 권한을 분리하지 않습니다. 최근 활동은 서버가 기록하지만 별도의 위변조 방지 감사 시스템은 아닙니다.

프로필 선택 ID만 sessionStorage에 보관하고 로그아웃 시 삭제합니다. 팀 데이터는 화면 메모리에 유지합니다. 아바타는 제공 목록만 사용하고 외부 이미지나 사진 업로드를 허용하지 않습니다. 사용자 텍스트는 HTML로 실행하지 않고 이스케이프하여 표시합니다.

배포 전 기존 기록을 검증하고 같은 비공개 저장소에 정확한 사본을 보관합니다. v1에서 v2로 첫 저장 시에도 원본 백업을 확인합니다. 구버전 클라이언트의 저장은 거절해 새 필드가 누락되는 것을 방지합니다. 백업·GitHub 이력은 휴지통 삭제나 만료로 지워지지 않습니다.

## 훈련 게임의 경계

- 모든 훈련 API에도 기존 세션·Origin·요청 제한을 적용합니다. 공개 정적 게임 규칙에는 연결키가 없습니다.
- 서버 시작 도전권은 규칙 버전·세션 ID·출처·프로필·게임·모드·조작·시드·성장 수치·봉인 단계·15분 유효 시간을 HMAC으로 묶습니다.
- 서버는 제한된 입력 기록을 같은 규칙으로 재계산하고 XP를 결정합니다. 요청의 임의 점수·XP와 프로필의 편집 가능한 설정 능력치를 사용하지 않습니다.
- 유효한 도전 ID의 결과 영수증과 SHA 충돌 재시도로 중복 지급과 덮어쓰기를 방지합니다. 유효 영수증은 한도 때문에 조용히 제거하지 않습니다. 도전 만료·용량·형식 오류는 저장하지 않고 실패합니다.
- 훈련은 고정된 파생 경로 `ARCHIVE_PATH + ".training.json"`에만 저장합니다. 이동 입력·도전권을 영구 저장하지 않고 최고 기록·XP·최근 요약·봉인·중복 방지 결과를 보관합니다.
- **서명과 입력 재계산은 완전한 부정행위 방지가 아닙니다.** 입력 타이밍·조작 모드는 브라우저가 보고하며 공개 규칙의 자동화도 가능합니다. 공용 프로필 선택은 본인 인증이 아니고, 공동 봉인의 서로 다른 세 프로필이 실제 세 사람이라는 보장은 없습니다.
- 훈련 파일은 기존 팀 기록의 화면 백업에 포함되지 않습니다. 배포 전 존재하는 파일을 검증하고 확인한 SHA의 정확한 비공개 사본을 보관합니다. 손상된 파일을 빈 파일로 덮어쓰지 않습니다.
- 게임 시계 진행용 엔드포인트는 로컬 합성 QA 서버에만 존재하며 운영 Worker의 라우트나 정적 배포 폴더에 포함되지 않습니다.

## 살아 있는 기록실의 경계

- 기존 세션·Origin·요청 제한을 사용하는 별도 `/api/room`으로 접근합니다. 저장 경로는 `ARCHIVE_PATH + ".room.json"`이며 기존 팀/훈련 기록을 변경하지 않습니다.
- 현재 팀에 존재하는 선택 프로필을 확인하고 단서·차례·그림 좌표·미로 통로/경로를 서버에서 검증합니다. 프로필은 본인 인증이 아닙니다.
- 물건 주인과 그림 원본은 놀이 진행에 따라 응답에서 가립니다. 프로필 변경과 공개 소스 확인이 가능하므로 엄격한 정답 비밀 유지나 개인 접근 권한 분리가 아닙니다. 사건 답은 미리 작성되어 있습니다.
- 고정한 요청 ID와 본문으로 15분 내 재시도하며, SHA 충돌 병합과 유효 영수증으로 중복 작업을 막습니다. 브라우저 작업 수신 전에 시작한 오래된 조회는 새 저장 상태를 덮어쓰지 못하도록 무효화합니다.
- 누락된 파일은 읽기만으로 초기화하지 않습니다. 손상된 파일·유효 영수증 포화·용량 초과는 실패 처리하며, 배포 전 존재하는 공간 파일도 정확한 비공개 백업을 확인합니다.
- 소름 연출 선택도 sessionStorage에 두고 로그아웃하면 지웁니다. 그림 초안은 메모리에만 유지하며 자동 영구 초안 저장을 보장하지 않습니다.
- 기존 화면의 팀 JSON 내보내기는 훈련/공간 파일을 포함하지 않습니다. 데이터 삭제는 GitHub 과거 이력/백업 삭제와 다릅니다.

## 모험과 필수 활동 프로필

- 로그인 후의 프로필 선택 팝업은 활동 기록의 명확성을 위한 UI 절차입니다. 서버의 개인 인증이나 역할별 권한으로 취급하지 않습니다.
- 모험은 기존 세션·Origin·요청 제한 아래 `ARCHIVE_PATH + ".adventure.json"`에 저장합니다. 개인 원정도 다른 활동 프로필로 선택하면 볼 수 있으므로 민감한 개인 비밀을 넣지 마세요.
- 현재 팀원, 각자의 이동 전 위치, 갈 수 있는 장소, 수집한 단서, 장치 답, 결말 투표 과반수를 서버에서 확인합니다. SHA 충돌은 최신 상태를 다시 읽고 작업 단위로 병합합니다.
- 15분 재시도 영수증은 유효 기간 중 임의로 제거하지 않습니다. 누락된 파일의 읽기 초기화, 손상 파일의 빈 상태 덮어쓰기, 용량 초과 저장을 하지 않습니다.
- 강화 훈련은 난이도 버전을 서명한 도전권으로 재계산하고 새 순위 키를 사용합니다. 이전 훈련 파일의 스키마·XP·역대 기록은 보존합니다. 이는 입력 자동화까지 막는 완전한 부정행위 방지가 아닙니다.

## 세션

- 서명된 호스트 전용 쿠키 `__Host-theme_sa`
- `HttpOnly`, `Secure`, `SameSite=Strict`, `Path=/`
- 기본 12시간 유효, 최대 설정값 24시간
- 요청 호스트와 유효 기간, HMAC 서명 검증
- 비밀번호가 변경되면 이전 쿠키 서명 검증 실패
- 새 배포마다 세션 서명 비밀 값을 자동 교체하여 기존 로그인 만료

일반 JavaScript는 HttpOnly 쿠키 값을 읽을 수 없습니다. 하지만 XSS가 있으면 쿠키를 읽지 않고도 사용자의 권한으로 요청할 수 있으므로 화면 코드의 변경 권한을 제한해야 합니다.

로그아웃은 현재 브라우저의 쿠키를 삭제합니다. 이 앱은 별도 세션 데이터베이스를 사용하지 않으므로, 이미 탈취되어 복사된 쿠키를 개별적으로 폐기하는 기능은 없습니다. 탈취가 의심되면 비밀번호 변경·재배포로 전체 세션을 만료시키세요. 이미 열람하거나 내려받은 기록은 회수할 수 없습니다.

## 서버 보호

- 로그인되지 않은 `/api/archive` 요청 차단
- 변경 요청의 Origin 검사와 교차 사이트 요청 거절
- API 응답 `no-store`, 정적 HTML `no-cache`
- 서버에서 UTF-8 JSON, 요청 크기, 팀원 수와 모든 필드 범위 검증
- 고정된 비공개 저장소·브랜치·주 기록/훈련/공간 경로 및 서버가 파생한 원본 SHA별 백업 경로만 접근
- GitHub 파일 SHA를 사용해 조용한 덮어쓰기 방지
- GitHub 저장소가 Public이면 접근 중단
- 로그인 및 API 속도 제한 바인딩이 없으면 실패 처리
- 서버 오류에 비밀번호·토큰·쿠키·팀 기록을 출력하지 않음
- `X-Frame-Options: DENY`와 `frame-ancestors 'none'`으로 외부 프레임 포함 제한
- 브라우저의 API 연결은 같은 출처로 제한

로그인 제한은 IP 및 Cloudflare 위치별 한도이며, 같은 네트워크 사용자가 한도를 공유할 수 있습니다. Cloudflare 바인딩의 제한은 위치별·최종 일관성 방식이며 모든 분산 공격을 엄격히 차단하는 전역 제한은 아닙니다. 길고 고유한 비밀번호와 운영 사용량 점검이 필요합니다.

## 비밀 값

GitHub Actions Secrets:

- `CLOUDFLARE_API_TOKEN`: 대상 Cloudflare 계정의 Worker 배포에 필요한 최소 권한
- `SITE_PASSWORD`: 공용 로그인 비밀번호
- `DATA_REPO_TOKEN`: `Theme-Sa-data` 하나만 허용한 GitHub Fine-grained 토큰, Contents 읽기·쓰기

배포 시 생성되는 `SESSION_SECRET`은 Worker Secrets에만 전달됩니다. 임시 비밀 JSON은 러너의 임시 경로에 만들고 배포 후 삭제합니다. 공개 자산이나 Actions 산출물로 업로드하지 않습니다.

Cloudflare Account ID와 GitHub 저장소 좌표는 비밀 값이 아닙니다. 하지만 API 토큰을 Account ID나 공개 Variables 자리에 잘못 넣지 마세요.

Fine-grained 토큰 접두어만으로 선택 저장소의 최소 범위를 확인할 수는 없습니다. 관리자가 GitHub 토큰 설정에서 데이터 저장소 하나만 선택했는지 확인해야 합니다. 서버는 고정된 기록·훈련·공간 파일 및 파생 백업 경로만 사용하지만 토큰 자체의 Contents 권한은 선택 저장소 내 다른 파일에도 적용되므로 데이터 전용 저장소를 사용하세요.

## 정적 파일

`site` 폴더만 정적 자산으로 올립니다. 서버 코드는 Worker 스크립트로만 배포합니다. `.assetsignore`와 공개 파일 검사로 이전 vault·개발용 비밀 파일의 게시를 방지하고 Worker에서도 관련 URL을 거절합니다.

우클릭·F12 제한은 사용자 인터페이스의 가벼운 제한입니다. 소스를 완전히 숨기지 못하며 인증·권한 제어의 일부로 간주하지 않습니다.

## 개인정보

기록은 Cloudflare 서버를 거쳐 GitHub에 저장됩니다. 종단간 암호화 저장소는 아닙니다. 실제 상세 집주소를 기록하지 말고 다른 개인정보도 필요한 범위에서 동의를 받아 사용하세요.

앱의 삭제는 현재 JSON에서 항목을 제거할 뿐입니다. GitHub의 과거 커밋, 클론, 백업, 사용자 사본까지 지우지는 않습니다. 데이터 저장소를 Public으로 바꾸면 과거 이력도 공개될 수 있으며 앱의 거절 동작이 GitHub 자체 공개 설정을 되돌려 주지는 않습니다.

## 사고 대응

1. 노출된 토큰은 해당 서비스에서 즉시 폐기합니다.
2. 비밀번호와 필요한 연결키를 교체하고 재배포해 세션을 만료시킵니다.
3. GitHub의 팀 기록과 커밋 이력을 확인합니다.
4. Cloudflare 배포·코드 변경과 사용량을 확인합니다.
5. 이미 노출된 정보가 있다면 영향을 받은 사람에게 알리고 사본·이력 정리를 별도로 진행합니다.

새 Cloudflare 주소가 검증되면 이전 GitHub Pages 사이트와 사용하지 않는 과거 배포 흐름을 정리하세요. 이전 Pages 워크플로를 재실행하면 과거 인증 방식의 사이트가 다시 게시될 수 있습니다.
